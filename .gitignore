# Tree state auto-backups (runtime)
**/tree-state.backup.*.auto.json

# Test sandbox backups
release/tree-system-v0.2.2/test-sandbox/*.bak

# TAO engine debug dumps
tao-engine/

# 临时工具打包（不进版本管理）
session-cleaner.zip

# 商业版原文件（不放 git，仅本地 dist）
main.cjs
preload.cjs

# 运维脚本备份（nudge-cleanup B3 / 引擎修复，2026-07-03）
**/tree-state.json.bak-*-pre-nudge-cleanup
tree-engine.cjs.bak-*-pre-*

# 手工操作前快照备份（restore / audit-gate edit 等，运行时产物，不版本管理）
**/tree-state.backup.*.before-*.json

# 补丁/引擎修复前整文件备份（ISS 修复 / improvement 等各轮次）
*.bak-pre-*

# Layer A 运行时调用日志（高频变化，不版本管理；分析用 scripts/tree-analyze.cjs）
**/call-log.jsonl*

# OS files
.DS_Store
Thumbs.db
assets/node_modules/
