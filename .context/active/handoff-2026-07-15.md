# 交接文档 — tree-harness 新主会话接手

> 交接时间: 2026-07-15 00:16 | 交出方: tree-harness 子会话（父会话 bbdefd1e 派生的 implement 子会话）
> 接手方: 新主会话 | 项目: tree-harness（Proma 树形会话执行体系改造）
> 权威源: `D:/codes/tree-harness/` | 当前分支: `release-0.13.16-hardening` | 最新 commit: `558a169`

---

## 〇、30 秒上手

你是 tree-harness 项目的**新主会话（编排者）**。项目状态：**Sprint 1-6 + e2e03 端到端验证全部完成并已提交（`558a169`，工作区干净）**。树形体系**核心机制已验证可正常工作**（GLM-5.2 自主跑通端到端），剩边角场景实证 + 产品化。

**先做这 3 件事恢复 context**：
1. 读 `D:/codes/tree-harness/CLAUDE.md`（P0 教训 + pro 操作要点 + 部署口诀）
2. 读 `D:/codes/tree-harness/.context/note.md` **顶部 2 条**（e2e03 验证 + Sprint 6 准备）
3. 读 `D:/codes/tree-harness/.context/active/e2e03-verification-2026-07-14.md`（最近一次端到端实证，新机制的 ground truth）

然后从本文档「七、后续工作清单」选一项开工。**建议首做 P0 e2e04（负面场景验证）**——补齐 e2e03 没演练的边角，性价比最高。

---

## 一、项目一句话定位

基于 Proma 商业版（AGPL-3.0），用 **sed 补丁 + 独立插件文件 + 内联引擎**叠加闭源能力，构建**树形多 agent 会话执行体系**（tree-system）。核心策略：开源做壳，闭源做肉。权威源 `D:/codes/tree-harness/`（2026-07-09 源码统一后确立）。

---

## 二、当前状态（2026-07-15）

- **Sprint 1-6 全完成**，全量测试 **367/0 零回归**。最新提交 `558a169`（Sprint 2-6 成果 + e2e03 一次性存档，89 文件 +17884/-311）。
- **e2e03 端到端真实项目验证通过**（2026-07-14）：GLM-5.2 commander 自主跑通 1 root + 3 worker(create_session 派生) + 1 auditor，全 done，产出 `03_ARCHITECTURE/` 三份形式化产物（1157 行）。Sprint 1-5 新机制实证 6/8 触发。
- **引擎/patches 已部署 pro 验证**：`tree-engine.cjs` md5 `5532fa5f`（5216 行）/ `proma-dev-patches.cjs` md5 `339082af`（3342 行）/ commander SKILL `8b2d20f2`。**三者必须一致=权威源**。
- 工作区**干净**（`git status` 无输出）。

---

## 三、🔴 必读红线（违反必出事）

1. **macp2 SubAgent 红线**（CLAUDE.md §P0）：SubAgent **只能用进程内 `Agent` 工具**；🚫禁 `create_session`/`fork_session`/`delegate_agent` 当 reviewer（建真实会话=烧钱，macp2 事故 4 分钟炸 207 会话打负 DeepSeek）。撞错修根因，禁换名重试。
2. **监督 tree-system 读 tree-state.json（文件通道），禁用 API `list_messages` 消息数判进度**（e2e03 教训）：GLM commander 在单个长未提交轮内完成全部工作，API `total=1` 是正常假信号≠卡死。判死活：`remote_get_session_info` status=busy + tree-state 是否推进，二者皆停才算真卡死。
3. **remote 调用方受限（D1-A）**：编排方对 pro 仅 `remote_send_message` 可用；`archive_session` 等被 `R6-external-deny` 拒。清理 pro 会话走 pro 本地。
4. **验证任务禁改引擎/patches/SKILL**（367/0 基线不动）。e2e 类测试只产文档/跑协议，不改代码。
5. **`ENGINE_PATH` 必须指权威源** `D:/codes/tree-harness/tree-engine.cjs`（md5 `5532fa5f`），禁指 `workspace-files/` 旧源（md5 `42ba5d51`，无 Sprint 1-5 改动=假绿灯根因）。

---

## 四、pro 实例操作要点（e2e 测试在此跑）

| 项 | 值 |
|---|---|
| 实例 | pro（`127.0.0.1:19877`，ISOLATED=1）|
| userData | **`~/.proma-dev/`**（⚠️非 .proma-pro；ARCHITECTURE.md §3.1 表过时待修）|
| 默认 workspace | `eb5e3f9c-d16c-419d-afcb-19e89c4114ee`（slug=default）|
| channel/model | `57d0f98e-abf2-43ca-bc48-837fe2decd88`（GLM）/ **GLM-5.2** |
| tree 目录 | `C:\Users\sir_c\.proma-dev\agent-workspaces\default\.context\trees\<tree_id>\` |
| 验证通道 | **直读 tree-state.json**（编排方能访问 `~/.proma-dev/`，每次 tool call 原子落盘）|

**建 commander 标准流程**（参照 e2e03）：
1. `remote_list_workspaces(pro)` 确认 workspace_id（remote_create_session 必传）
2. `remote_create_session(pro, channel_id=57d0f98e, model_id=GLM-5.2, workspace_id=eb5e3f9c, title=...)`
3. `remote_send_message(commander, wait=false)` 下发自包含 7 段式 prompt（含显式冷启动协议步骤）
4. **sleep 90-120s + 直读 tree-state.json 轮询**（勿依赖 API 消息数）；要精确结论发简短问题 wait=true

**派 worker 源材料**：pro workspace 默认无 D:/codes 访问权。需先把源文档 cp 到 pro `workspace-files/.context/<tag>-sources/`（e2e03 这么做的）。

---

## 五、Sprint 1-5 新机制速查（e2e03 实证状态）

| 机制 | Sprint | e2e03 实证 | 说明 |
|---|---|---|---|
| 5 件套持久化 | S2 | ✅ 触发 | brief/dod/report_protocol/autonomy/self_audit 进 leaf |
| create_session 派生（非 fork）| S2 | ✅ 触发 | session_registry source=create_session |
| done 门禁 8 道 | S1 | ✅ 触发 | E_GATEKEEPER_REQUIRED 实证 gate 7 |
| auditor role + 交叉审 | P0a/S5 | ✅ 触发 | role=auditor 简化协议，11pass/3fail |
| max_sessions 护栏 | S5 | ✅ 就位 | count=5≪50，E_MAX_SESSIONS 未触上限 |
| drift 三档联动 | S2 | ⚪ 未触发 | clean run 无 drift，**待 e2e04 演练** |
| flagged 篡改检测 | S1 | 🟡 未触发 | 无篡改场景，**待 e2e04 演练** |
| E_MAX_SESSIONS 硬护栏 | S5 | ⚪ 未触发 | 未超量，**待 e2e04 演练** |

---

## 六、e2e03 遗留小项（低优先，可顺手清）

1. **e2e03 产物 3 处已知瑕疵**（auditor 抓出，verbatim 保留在 `03_ARCHITECTURE/`）：
   - `sequence-diagrams/03-prune-cascade.puml` L64：drift kind=`escalation` 越界 → 应为 `production`/`direction`/`rhythm`
   - `sequence-diagrams/04-ctx-segment.puml` L47：session_registry source=`segment` 越界 → 用合法 source 枚举
   - `data-model.md` 附录 A.3：LEAF_NAME_RE 段3 role 描述与 §3 ROLE_ENUM 自相矛盾
2. **ARCHITECTURE.md §3.1**：pro userData 标 `.proma-pro/` 过时，应改 `.proma-dev/`（CLAUDE.md 已标，待修 ARCHITECTURE）。
3. pro 残留 2 个 idle 误建会话（driver `b72b1d16` / ping `d9b6fb9c`，e2e03 误判时建），remote 无法归档，走 pro 本地清。

---

## 七、后续工作清单（优先级排序）

### P0 — e2e04 负面场景验证（最性价比，补齐 e2e03 边角）
在 pro 构造 3 个负面场景各跑一次，验证未触发的机制实战有效：
- **场景① drift 三档联动**：让 worker 故意越界（如产出的内容超出 brief.in_scope），触发 commander 三档纠偏（nudge→limit→prune），验证 drift_history 留痕 + 级联。
- **场景② flagged 篡改检测**：worker done 后，绕过引擎直接编辑 tree-state.json（伪造 audit_gate=pass），验证 Layer3 W-AUDIT-* 事后检测 + flagged 拦截。
- **场景③ E_MAX_SESSIONS 硬护栏**：把 audit_meta.max_sessions 调小（如 3），让 commander 派第 4 个 worker，验证 E_MAX_SESSIONS 拦截 + session_registry count。
- 产出：`.context/active/e2e04-verification-2026-07-15.md`（负面场景实证报告）。

### P1 — 修 e2e03 遗留 3 瑕疵 + ARCHITECTURE §3.1（见上节，小改，~10 分钟）

### P2 — 团队维度对照实验（success-metrics 外部依赖）
`05_PROJECT_PLAN/experiment-design.md` 设计的 5 任务 × 3 模式 × 3 重复 = 45 次，验证多 agent vs 单 agent。外部依赖（~$45-225）。聚合脚本 `aggregate-metrics.cjs` + n=1 数据 `metrics-n1-20260714.json` 已就绪。

### P2 — 首个上游 PR
上游实为 **`proma-ai/Proma`**（AGPL-3.0，Bun monorepo + TS 源码，已迭代到 v0.13.3；tree-harness 补丁针对 v0.12.x，版本不同步）。首个 PR 路径：clone 上游 → 找 v0.13.x 真实改进点（方向：MCP 会话频道/模型一致性，补丁 C 上游化）→ fork `orphiczhou/Proma` → 适配 TS PR。详见 `05_PROJECT_PLAN/first-pr-draft.md`。提交=外部。

### P3 — 跨仓根治（长期，单边缓解已做）
- fork identity timeout（BUG-A，Proma app 层）
- SDK create_session 回调钩子（聚类 A，Layer4 platform 层）
- subagent_trace_id（Layer4 capability-based 调用 + event hash chain）

---

## 八、关键资源指针

| 文档 | 路径 | 用途 |
|---|---|---|
| 项目知识库 | `D:/codes/tree-harness/CLAUDE.md` | P0 教训 + pro 要点 + 部署口诀 |
| 长期笔记 | `D:/codes/tree-harness/.context/note.md` | 按日期追加，最新在顶 |
| e2e03 实证 | `D:/codes/tree-harness/.context/active/e2e03-verification-2026-07-14.md` | 最近端到端，机制 ground truth |
| 架构 | `D:/codes/tree-harness/ARCHITECTURE.md` + `03_ARCHITECTURE/` | 三层架构 + 形式化产物（class/data/sequence）|
| API | `D:/codes/tree-harness/API.md` + `ERROR-CODES.md` | 53 工具 + 51 错误码 |
| commander 手册 | `D:/codes/tree-harness/skills/tree-commander/SKILL.md` | §4 流程 / §13 冷启动 / §13.5 SubAgent 红线 |
| Sprint 计划 | `D:/codes/tree-harness/05_PROJECT_PLAN/sprint-plan.md` | 路线图 + 交付物状态 |
| 缺口分析 | `D:/codes/tree-harness/.context/active/design-files-mapping-2026-07-09.md` | 文档缺口 P0-P2 清单 |
| BUG 注册表 | `D:/codes/tree-harness/BUG-REGISTRY.md` | BUG 全局编号体系 |

---

## 九、新主会话第一步建议

1. 读「〇、30 秒上手」3 份文档恢复 context
2. 与用户确认选哪个后续工作（默认推荐 P0 e2e04）
3. 若 e2e04：参照本交接「四、pro 操作要点」+「七 P0」三场景设计，在 pro 建 commander 跑
4. 每完成一项：更新 `note.md` 顶部 + 提交 git（项目惯例 commit message 中文 + Co-Authored-By）

---

> 本交接由 tree-harness 子会话于 2026-07-15 00:16 生成。父会话 `bbdefd1e`，本会话为 implement 子会话（e2e03 端到端测试编排）。新主会话接手后，本子会话可归档。
