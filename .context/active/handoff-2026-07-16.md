# 交接文档 — 2026-07-16 新主会话接手

> 交出方：主会话 a5c20252（GLM-5.2，proma workspace）| 2026-07-16 19:10
> 接手方：新会话（proma workspace）
> 项目：tree-harness（Proma 树形会话执行体系改造）
> 分支：release-0.13.16-hardening | 最新 tag：**v0.17.0** | 最新 commit：`a956cf6`

---

## 〇、30 秒上手

**v0.17.0 私有化发布 + §4.6 worker 自审实战验证成功 + nanju 项目设计阶段全完（01-06 ✓）**。tree-harness 从孵化到首次私有化发布 + 实战验证全闭环。v0.17.0 实战矩阵 4/4 全 ✓（prefix / leaf_add / §14.1 auditor / §4.6 自审）。

**先读 4 份**：
1. 本交接文档
2. `D:/codes/tree-harness/CLAUDE.md`（P0 教训：macp2/nanju/brief审计义务 + pro 操作要点 + 红线）
3. `D:/codes/tree-harness/.context/active/v0.17.0-verify-2026-07-16.md`（§4.6 验证完整报告 + 教训）
4. `D:/codes/tree-harness/.context/note.md` **顶部**（e2e04 / thdev / nanju 条目）

---

## 一、本会话（a5c20252）成果 commit 清单

| commit | 内容 |
|---|---|
| `eace551` | e2e04 负面场景验证（drift/flagged/max_sessions 三机制，Sprint 1-5 矩阵 8/8）|
| `02ed53e` | P1 修复 e2e03 瑕疵 + CLAUDE.md 跨树陷阱 + 发现C dead code |
| `c583559` | thdev W1 删 isFlagged dead code + W3 文档 |
| `f0f6ced` | W2 first-pr-draft 方向1 源码深化（stale-meta gap 成立）|
| `fbc353b` | 阶段总结文档 |
| `1305034` | nanju 教训固化（CLAUDE P0 + SKILL §14.1）|
| `60e07e7` | **prefix 前置校验**（引擎 cmdInit，防超长潜伏 leaf_add）|
| `3b0cf76` | commander SKILL 迁移（§14.1 nanju 根因+铁律 / §13.5 macp2 收敛）|
| `3fcaa25` | worker SKILL 迁移（§4.6 主动触发+收敛+prefix 防线）|
| `6256f1d` | SKILL 多维度审计报告（4pass/9yellow/0red）|
| `687b7a1` | SKILL 修复 6 处 + 收敛审计 pass（发布就绪）|
| `7e2b48f` | **v0.17.0 发布文档**（CHANGELOG/README/RELEASE_NOTES/DEPLOYMENT）|
| `a956cf6` | v0.17.0 实战验证报告（§4.6 自审 ✓）|

nanju 项目 commit（`D:/Codes/multi-agent-collab-platform/`）：`451b65b` / `7251e73` / `0849e41` / `f2f4989`

---

## 二、当前状态

### tree-harness
- **v0.17.0 tag** 发布（私有化，不推 GitHub）
- pro dist `3efe6a2b`（tree-engine 含 prefix 前置 + isFlagged dead code 清理）
- pro skills：commander `56664fbe` / worker `53cb5126`（v0.17.0 脱敏收敛）
- 全量测试 367/0 + prefix-init 17/0
- Sprint 1-5 机制实证矩阵 8/8（e2e03 + e2e04）
- v0.17.0 实战矩阵 4/4 ✓（prefix / leaf_add / §14.1 auditor / **§4.6 worker 自审**）

### nanju（D:\Codes\multi-agent-collab-platform）
- **设计阶段全完**（01_PRD / 02_UX / 03_ARCH / 04_API / 05_PLAN / **06_TESTS** ✓）
- commit `f2f4989`（06_TESTS 816 行 + §4.6 验证）
- audit 6 findings（04/05）大部分已修（nanju04 worker 修 + nanju05 补 06）
- 可进入**实现期 Sprint S1**（写代码，基于 proma-source 扩展）

---

## 三、🔴 红线（3 条，违反必出事）

1. **SubAgent 只用进程内 Agent 工具**（禁 `create_session`/`fork_session`/`delegate_agent` 当 reviewer——macp2 事故 4 分钟炸 207 会话）。派 worker 独立会话（create_session 当**工作者**合法，当 reviewer 禁）。
2. **监督读 tree-state events 详情**（不靠 `status`/`write_count`/API `total` 假信号）——**本会话误判 3 次**（auditor "卡"实则在长轮审/在跑；commander busy 不写 tree 但在处理）。**必读 events meta**（findings/alignment/red_count），不只看 status/total。
3. **不改引擎/patches/SKILL**（v0.17.0 基线不动）。验证任务禁改；开发任务（如 prefix 前置）走完整流程（改源 + 测试 + 部署 pro + 重启）。

---

## 四、后续工作清单（优先级）

### P0 — v0.17.1 小修
- **review_round reviewer_session_id 占位修复**（nanju05 worker 用了占位 UUID 11111111-...，SKILL §4.6 加约束"必须真实 SubAgent session_id"）
- **commander 监督策略优化**（长轮用 events 末尾 + last_event_type 双确认）

### P1 — nanju 实现期
- **Sprint S1**：设计阶段全完，开始写实现代码（基于 proma-source 扩展向导Agent/编程Agent/裁判Agent）
- nanju design 阶段 audit findings 剩余（如果有的话，看 .context/audit-*）

### P2 — 外部依赖（E1-E5）
- **E1 首个 PR**：方向1 stale-meta gap 已确认（first-pr-draft §九），建议 Issue 先行到 proma-ai/Proma
- E2 团队试用 / E3 对照实验（45 次 ~$45-225）/ E4 SDK 回调 / E5 fork identity（跨仓长期）

### P3 — 设计 vs 实现 gap（第 1 类真 gap）
- human_checkpoint（unified-workflow §八 5 介入点，设计完未落地，无 Sprint 承接）
- 中档限权替代（autonomy_override 删后未补）
- TaoWatcher file-log（Sprint 4 follow-up）

---

## 五、关键资源指针

| 文档 | 路径 | 用途 |
|---|---|---|
| 项目知识库 | `D:/codes/tree-harness/CLAUDE.md` | P0 教训 + pro 要点 + 红线 |
| 长期笔记 | `D:/codes/tree-harness/.context/note.md` | 顶部 e2e04/thdev/nanju 条目 |
| v0.17.0 验证 | `D:/codes/tree-harness/.context/active/v0.17.0-verify-2026-07-16.md` | §4.6 完整验证 + 教训 |
| 发布说明 | `D:/codes/tree-harness/RELEASE_NOTES.md` | v0.17.0 私有化发布范围 |
| commander SKILL | `D:/codes/tree-harness/skills/tree-commander/SKILL.md` | v0.17.0（§14.1 nanju + §13.5 收敛）|
| worker SKILL | `D:/codes/tree-harness/skills/tree-worker/SKILL.md` | v0.17.0（§4.6 主动触发）|
| nanju 项目 | `D:/Codes/multi-agent-collab-platform/` | 设计全完 01-06，可进入实现 |

---

## 六、pro 实例参数 + 操作要点

| 项 | 值 |
|---|---|
| 实例 | pro（`127.0.0.1:19877`，ISOLATED=1，userData `~/.proma-dev/`）|
| 启动 | `D:/Proma-dev/start-pro.bat`（需用户启动或 powershell Start-Process）|
| workspace | `eb5e3f9c`（slug=default）|
| channel/model | `57d0f98e`（GLM）/ **GLM-5.2** |
| tree 目录 | `~/.proma-dev/agent-workspaces/default/.context/trees/` |
| 验证通道 | **直读 tree-state.json**（leaves/events/session_registry，每次 tool call 原子落盘）|
| 红线 | remote 编排方仅 `remote_send_message` 可用；`archive_session` 等被 R6-external-deny |

**派 worker 子会话要点**（用户 2 次纠正，已入 memory）：
- `create_session(workspace_id=主会话workspace, ...)`——**必显式指定 workspace_id**（不指定默认去 default/南大项目工作区）
- 独立 Proma 会话（不 fork，fork 继承主会话历史太重）
- 隶属靠 tree leaf（`leaf_add added_by=root`），不靠 Proma session parentSessionId

**commander 长（GLM-5.2 读 SKILL/nanju 大）**：可能 10-15 分钟不写 tree（读/分析/思考），`busy` 但 `write_count` 不变——**长轮假信号，不是卡**。读 events 推进判活。

---

## 七、memory（跨会话）

`user-subagent-workspace-pref`（feedback 类）：派 worker 用独立 Proma 会话 + 显式同工作区 + 不 fork（用户 2 次纠正 2026-07-15）。

---

> 本交接由主会话 a5c20252 于 2026-07-16 19:10 生成。本会话从 e2e04 接手，完成 v0.17.0 发布 + §4.6 验证 + nanju 设计阶段，共 13 个 tree-harness commit + 4 个 nanju commit。
