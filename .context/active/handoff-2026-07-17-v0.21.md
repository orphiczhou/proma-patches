# 交接文档 — 2026-07-17 新主会话接手（v0.21 全闭环 + nanju S1 + 两 gap）

> 交出方：主会话 ed6cc5c8（GLM-5.2，proma workspace）| 2026-07-17 12:26
> 接手方：新会话（proma workspace）
> 项目：tree-harness（Proma 树形会话执行体系改造）+ nanju（多智能体协同平台）
> 分支：release-0.13.16-hardening | 最新 commit：`323e4ac` | 最新 tag：v0.17.0

---

## 〇、30 秒上手

**v0.17.1→v0.21 全闭环 + 分离 bug 修复 + nanju S1 首次真实项目代码实现完整闭环 + 两个 gap 浮现待修。** tree-system 从"纯 SKILL 教化"演进到"四引擎硬拦 + 多模型交叉 + 真实项目代码实现"，首次跑真实项目（nanju S1 向导引擎基础）产真实 TS 代码 ~45KB + DeepSeek 审 + 全 v0.21 链路生效。

**先读 4 份**：
1. 本交接文档
2. `D:/codes/tree-harness/CLAUDE.md`（P0 教训 4 条 + pro 要点 + 红线 + v0.18-v0.21 说明）
3. `D:/codes/tree-harness/.context/note.md` **顶部**（v0.21 nanjuS1 + 特派员回收 + 演进 + 分离 bug + v0.18-v0.21 全条目）
4. `D:/codes/tree-harness/CHANGELOG.md` [Unreleased]（v0.18-v0.21 + 分离 bug 条目）

---

## 一、本会话成果总览

### engine 改动（v0.17.1→v0.21，4 个"LLM 不可靠→引擎硬拦"候选全落地）

| 版本 | 改动 | 错误码 | engine md5 |
|---|---|---|---|
| v0.18 | worker role 禁 review_round session 分支（堵 GLM worker 占位/借真 UUID 蒙混）| E_REVIEW_SESSION_FORBIDDEN | 5532fa5f→e10f93b2 |
| v0.19 | root done 须子 done（L1762 扩 root，堵 commander 提前 done 放弃子任务）| E_CHILDREN_NOT_DONE（扩 root）| e10f93b2→51888140 |
| v0.20 | audit_gate red 阈值（audit_log findings red → 拒 pass）+ auditor 不同模型 + tree-auditor SKILL | E_AUDIT_RED_BLOCKED | 51888140→41c08f89 |
| v0.21 | pass_with_minor 枚举 + results severity 必填 + isPassVerdict helper + 报错一次性 schema + help topic | — | 41c08f89→**04a74e62** |

**当前 engine**：tree-engine.cjs **5388 行**，md5 **`04a74e62`**，44 个错误码。
**当前 patches**：proma-dev-patches.cjs **3346 行**，md5 **`5083480d`**（分离 bug 修复：treeDir 跟 PROMA_INSTANCE_NAME）。

### 分离 bug 修复（2026-07-16 发现 + 修复）

patches 4 处 treeDir（L295/1685/1830/2333）`isIsolated → 硬编码 .proma-dev` 改跟 `PROMA_INSTANCE_NAME`（pro→.proma-pro / dev→.proma-dev）。SKILL 部署 .proma-pro（commander workspace），tree 监督读 .proma-pro（分离 bug 修复后 session+tree 同 .proma-pro）。

### SKILL 改动

| SKILL | 版本 | 改动 |
|---|---|---|
| tree-commander | v2.6 | §13.4.1 auditor 用 create_session 不同模型（v0.20）/ §6 监督判活红线 / §13.5.2 session 分支 worker 禁 |
| tree-worker | v2.6 | §4.6 schema 位置红线（reviewer_kind reviewer 级 + worker 禁 session 分支）|
| **tree-auditor** | **v1.1（新）** | 独立方法论：G1-G5 + G5 安全 CWE（10 项）/ verdict 阈值表（red→required / mid 安全→pass_with_minor）/ 多模型交叉分档 / §3.1 加 pass 对齐 engine / §6.3 engine 真实模板 / §6.1 五步含 root 背书 |

### 测试

- sprint-v018 8/0 + sprint-v019 3/0 + sprint-v020 4/0 + sprint-v021 4/0
- iss003 + subagent-lifecycle（迁 subagent 分支）
- **全量 0 回归**（18 plan tests + dbc-spec 39/0）

### 实战验证（pro 端到端）

| 树 | 版本 | 结果 |
|---|---|---|
| v172t | v0.17.1 | 0 spawn 伪造（纯教化证伪）|
| v18t | v0.18 | 2 spawn subagent（worker 改善，commander root 提前 done）|
| v20t | v0.19+分离 bug | 全 done（spawn 5 + 3轮收敛 + root done 正确顺序）|
| v21t | v0.20 | auditor 不同模型（DeepSeek）✓ / 11 findings 无 severity（engine schema 冲突）|
| **v22t** | **v0.21** | **完美闭环：auditor 一次提交 + severity 全标 + pass_with_minor + 5 CWE** |
| **nanjuS1** | **v0.21** | **首次真实项目代码实现：worker 产 4 TS 文件 ~45KB + DeepSeek 审 24 findings + 全 done** |

### git 提交

- commit `323e4ac`：v0.18-v0.21 引擎硬拦 + 分离 bug + tree-auditor SKILL（41 files, +2024/-237）

---

## 二、当前状态

### tree-harness
- 分支 release-0.13.16-hardening | commit `323e4ac`
- pro dist engine `04a74e62` + patches `5083480d` + SKILL v2.6/v2.6/v1.1
- 全量测试 0 回归 + sprint-v018/v019/v020/v021 全绿

### nanju（D:\Codes\multi-agent-collab-platform）
- 设计阶段 01-06 全完
- **S1 向导Agent引擎基础代码产出**（deliverables/src/ 4 TS 文件 ~45KB，在 .proma-pro/.../trees/nanjuS1/deliverables/src/）：
  - mode-selector.ts (8582B) / requirement-collector.ts (12800B) / types.ts (9599B) / wizard-engine.ts (14492B)
- tsc --strict 零类型错误，架构对齐
- 待集成到 nanju src（D:\Codes\multi-agent-collab-platform\src\）

---

## 三、🔴 红线（3 条，违反必出事）

1. **SubAgent 只用进程内 Agent 工具**（禁 create_session/fork_session/delegate_agent 当 reviewer——macp2 事故 4 分钟炸 207 会话）。派 worker 独立会话（create_session 当**工作者**合法，当 reviewer 禁）。
2. **监督读 tree-state events 详情**（不靠 status/write_count/API total 假信号）——长轮 10-15 分钟不写 tree 可能读 SKILL/思考，非卡。
3. **不改引擎/patches/SKILL 基线不验证**（v0.21 已部署 pro）。改走完整流程（源+测试+部署 pro+重启）。

---

## 四、后续工作清单（优先级）

### P0 — 两个 gap（v0.22 候选，特派员报告浮现）

#### Gap A：yellow 修复驱动力不足
- **问题**：worker "非 red 不修"是系统问题——9 yellow 进入"已知但未修复"真空（nanjuS1 实战暴露）
- **修复方向**：DoD 加 `yellow_count=0 或有 S2 修复计划`（闭环 yellow）；或 engine 加 yellow_count 门禁
- **SKILL**：tree-auditor §4 verdict 阈值 + tree-worker done 前置加 yellow 处理

#### Gap B：auditor→worker 反馈闭环缺失
- **问题**：auditor 发现问题（findings）但无 fixer 角色回修。worker done 后 auditor findings 无人修
- **修复方向**：post-audit fixer 角色（auditor findings → fixer 修 → auditor 复审）；或 commander §14 审计工作流加 fix leaf（§14.2 已有 fix 修正员，但 §13.4 独立审 worker 缺 fix 闭环）
- **引擎**：可能加 audit_findings_resolved 事件（类似 P1b fix_evidence）

### P1 — nanju S1 其他模块（commander 多路并行）

nanju S1 = 地基与最窄骨架（模式选择→对话→首快照）。已完：向导Agent引擎基础。剩余模块：
- **T6 快照管理器基础**（fs.linkSync 硬链接 + 首快照含项目元数据+目录骨架）
- **T7 裁判引擎/编程引擎占位桩**（仅接口定义，不实现逻辑）
- **T1-T5**（UI/路由/埋点/其他向导任务，看 sprint-plan.md S1 任务表）

**建议**：commander 多派几路 worker 并行（T6 快照 + T7 占位桩 + 其他），加速 S1 完成。参照 nanjuS1 模式（worker 产代码 + DeepSeek auditor 审 + v0.21 全链路）。

### P2 — nanju 代码集成

nanjuS1 deliverables/src/ 4 TS 文件集成到 `D:\Codes\multi-agent-collab-platform\src\`：
- 修 9 yellow（特派员发现：README 方法名/MODE_KEYWORDS 导出/'go ' 尾空格/review_round 选择性收录 + 其他 5 条）
- MODE_KEYWORDS 用词边界替代 trim / README 路径去绝对化 / GUIDE_QUESTIONS 长度断言
- S2 替换 stub 为真实 LLM / nextQuestion 利用 ambiguities

### P3 — 其他
- Gap2 auditor 时机（role=auditor 双语义误伤 §14，需 auditor 子类型字段）
- Gap3 worker 状态（pending_brief→done 合法，强制 active 破坏）
- root done 门禁 vs §13.3a（root done 要 milestone/deliverables，但 §13.3a 说不走八步——SKILL vs 引擎不一致，backlog）

---

## 五、关键资源指针

| 文档 | 路径 | 用途 |
|---|---|---|
| 项目知识库 | `D:/codes/tree-harness/CLAUDE.md` | P0 教训 4 条 + pro 要点 + 红线 + v0.18-v0.21 |
| 长期笔记 | `D:/codes/tree-harness/.context/note.md` | 顶部 v0.21 nanjuS1 + 特派员 + 演进 + 分离 bug |
| CHANGELOG | `D:/codes/tree-harness/CHANGELOG.md` | [Unreleased] v0.18-v0.21 |
| commander SKILL | `D:/codes/tree-harness/skills/tree-commander/SKILL.md` | v2.6（§13.4.1 不同模型 / §6 监督判活）|
| worker SKILL | `D:/codes/tree-harness/skills/tree-worker/SKILL.md` | v2.6（§4.6 schema 位置红线）|
| **auditor SKILL** | `D:/codes/tree-harness/skills/tree-auditor/SKILL.md` | **v1.1**（独立方法论，§3.1 加 pass / §6.3 engine 模板 / §4 pass_with_minor）|
| plan 文件 | `.context/virtual-sprouting-crescent.md` | v0.21 engine plan（pass_with_minor + severity 必填）|
| nanju 项目 | `D:\Codes\multi-agent-collab-platform\` | 设计 01-06 + S1 向导引擎基础代码 |

---

## 六、pro 实例参数 + 操作要点

| 项 | 值 |
|---|---|
| 实例 | pro（`127.0.0.1:19877`，ISOLATED=1）|
| 启动 | `D:/Proma-dev/start-pro.bat`（PROMA_INSTANCE_NAME=pro + PROMA_DEV=1）|
| workspace | `eb5e3f9c`（slug=default）|
| channel/model | `57d0f98e`（GLM）/ GLM-5.2 ; `d54cd84a`（DeepSeek）/ deepseek-v4-pro |
| **SKILL 部署** | `~/.proma-pro/agent-workspaces/default/skills/`（**不是 .proma-dev**，分离 bug 修复后）|
| **tree 目录** | `~/.proma-pro/agent-workspaces/default/.context/trees/`（分离 bug 修复后 tree 也落 .proma-pro）|
| 验证通道 | 直读 tree-state.json（.proma-pro）|
| engine md5 | **04a74e62** / patches **5083480d** |

**派 worker 子会话要点**（memory `user-subagent-workspace-pref`）：
- `create_session(workspace_id=主会话workspace, ...)`——必显式指定 workspace_id
- 独立 Proma 会话（不 fork）
- 隶属靠 tree leaf（`leaf_add added_by=root`）

**派 auditor 不同模型**（v0.20 §13.4.1）：
- `create_session(channel_id="d54cd84a-...", model_id="deepseek-v4-pro", ...)` 派 DeepSeek auditor
- auditor 用 Skill 工具加载 tree-auditor v1.1

---

## 七、memory（跨会话）

- `user-subagent-workspace-pref`（feedback）：派 worker 用独立 Proma 会话 + 显式同工作区 + 不 fork

---

> 本交接由主会话 ed6cc5c8 于 2026-07-17 12:26 生成。v0.17.1→v0.21 全闭环 + 分离 bug + nanju S1 首次真实项目 + 两 gap 待修。
