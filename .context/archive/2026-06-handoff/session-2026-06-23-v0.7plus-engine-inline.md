# 交接：v0.7+ 引擎内联 MCP — 消除工作区源码暴露（2026-06-23）

> 上一会话进度交接 | 工作区: Proma改造探索 | 用户: 周星星
> 入口: 读本文件 + PROJECT-INDEX.md + note.md 5 分钟恢复

## 一句话状态
**tree 引擎已真正内联进 MCP（不再是 spawn 薄包装），工作区零源码泄漏；代码+部署+清理+文档全部完成，待用户重启 D:\Proma-dev 验证 27 个 mcp__tree__* 真注册。**

## 起因
commit ba2c030 的"MCP 化"被独立审计发现是半成品：`createTreeMcpServer` 只 spawn 包装 `node tree-state.js`，90KB 引擎源码仍躺在每个工作区 `.context/trees/`，agent 可 Read/Edit/cat 直接绕过 MCP。本次真正内联（对照 session-management 范本）。

## 本次完成（6 任务 + 部署，方案见 `.context/plan/tree-engine-inline-mcp.md`）

| 任务 | 内容 |
|---|---|
| M1 | tree-state.js(2428行)→`patch-l/tree-engine.cjs`: TREES_ROOT 改可注入(setTreesRoot) + run(cmd,args,treesRoot?)返回{ok,error?}与CLI等价 + CLI shim + exports。**cmd/12DbC/文件锁零改动**(A1 diff 实证) |
| M2 | patches.cjs: callTreeState spawn→engine.run(per-call treesRoot); W-08改查 mcp__tree__tree_*写工具; C-11简化查直接读写tree-state.json |
| M3 | dbc-spec/audit-attacks: execFileSync→require engine.run(async化) + _findEngine()自适应查找 |
| M4 | commander SKILL.md: 20+处 CLI→mcp__tree__*(26/27); assets/自包含 dbc-spec+audit-attacks |
| MED M1 | run() 加 per-call treesRoot + try/finally恢复(MCP并发显式安全) |
| A1 | collaboration 独立审计子会话 → 结论"代码可部署"，发现 B1(部署未完成→已修)+tree-2遗留(已清理) |

## 验证（四重）
- smoke 12/0(M1) + 11/0(MED M1 per-call隔离)
- dbc-spec **21/0** + audit-attacks **18攻击/CRITICAL=0**（3 BYPASS+2 GAP 全是 V4-V8既有项，非本次引入）
- A1 独立审计通过
- **工作区 .context/trees/ 全局零 .js 源码**（proma/tree-1/tree-2 的 tree-state.js 已删，数据保留）

## 部署状态
- ✅ dist/: `patches.cjs`(新版)+`tree-engine.cjs`(新增) → `D:/Proma-dev/resources/app/dist/`（旧版备份 .bak-20260623-pre-inline）
- ✅ 激活skill: SKILL.md 514行CLI版→744行MCP版（`~/.proma/agent-workspaces/proma/skills/tree-commander/`，备份.bak）
- ✅ assets 同步两处(workspace-files+激活)，findEngine 自适应
- ✅ 清理 3 处工作区遗留 tree-state.js
- ⏳ **未重启验证**（需用户）

## 关键文件
| 文件 | 源 | 部署 |
|---|---|---|
| tree-engine.cjs | release/tree-system-v0.2.2/patch-l/ | D:/Proma-dev/resources/app/dist/ |
| proma-dev-patches.cjs | 同上 | 同上 |
| dbc-spec/audit-attacks | release/.../test-sandbox/ | skills/tree-commander/assets/(两处) |
| commander SKILL.md | workspace-files/skills/tree-commander/ | ~/.proma/.../skills/tree-commander/(激活) |
| 原 tree-state.js（Phase A 源）| release/.../core/tree-state.js | **保留**(engine.cjs源头+基线对照) |

## 关键技术决策（新会话必读）
1. **spawn→require**: spawn 保留文件暴露；require 内联消除。`__dirname`障碍用 setTreesRoot 注入解决
2. **run()等价CLI stdout**: MCP/dbc-spec 调用方零改动（只看{ok,error?}）
3. **per-call treesRoot**: MCP路径显式安全（不依赖模块级共享；当前临界区纯同步已安全，per-call是显式防御）
4. **findEngine自适应**: 验收工具在 test-sandbox/assets/dist/ 任意位置定位 engine
5. **CLI shim保留**: 向后兼容（`node tree-engine.cjs <cmd>`），过渡期可用

## 下一步（新会话）
1. 🔴 **用户重启 D:\Proma-dev\start-dev.bat**，新建 session，确认 27 个 mcp__tree__* 注册 + `tree_validate`/`tree_leaf_add`/`tree_audit_gate` 返回 {ok:true}
2. 验证后 git commit（本次改动：见下方清单）
3. V4-V8 深度加固（audit-attacks 的 3 BYPASS+2 GAP）：A3 alignment必填 / A5 验pass值 / CP2直接改文件(需Layer4) / A4 budget0 / MS audit_pass鉴权
4. Phase D（D1/D2/D3用户层bug）+ Phase B-G + Layer 4 subagent_trace_id

## 本次改动文件清单（git commit 用）
- 新增: `patch-l/tree-engine.cjs`、`skills/tree-commander/assets/{dbc-spec,audit-attacks}.cjs`
- 修改: `patch-l/proma-dev-patches.cjs`、`test-sandbox/{dbc-spec,audit-attacks}.cjs`、`skills/tree-commander/SKILL.md`
- 文档: `.context/{note.md,PROJECT-INDEX.md,plan/tree-engine-inline-mcp.md,handoff/session-2026-06-23-v0.7plus-engine-inline.md}`
- 删除: `proma/tree-1/tree-2` 的 `workspace-files/.context/trees/tree-state.js`
- 部署(非git): `D:/Proma-dev/dist/{patches,tree-engine}.cjs`、`~/.proma/.../skills/tree-commander/`
